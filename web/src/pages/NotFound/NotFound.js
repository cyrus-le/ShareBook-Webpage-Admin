import React from 'react';
import { Typography } from '@material-ui/core';

function Login() {
    // const styles = useStyles()

    return (
        <div style={{ width: '100%', backgroundColor: '#eeeeee' }}>
            <Typography variant="h1" style={{ margin: 0, textAlign: 'center', fontSize: '7rem', fontWeight: 600 }}>404</Typography>
            <Typography variant="h1" style={{ margin: 0, textAlign: 'center' }}>Page Not Found</Typography>
        </div >
    );
}

export default Login;